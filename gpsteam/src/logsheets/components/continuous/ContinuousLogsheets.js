/*For the bug (is not a property), refer to Sites.js*/

import React, { Component } from 'react'
import ConLogMod from './ConLogMod'

class ContinuousLogsheets extends Component {

    render() {
        return (
            <ConLogMod/>
        );
    }
}

export default (ContinuousLogsheets)